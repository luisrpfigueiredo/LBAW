<?php
function createTags($question_id, $tags)
{
    global $conn;

    /*$stmt = $conn->prepare("INSERT INTO tag (user_id, title, body) VALUES(:user_id, :title, :body)");
    $stmt->execute($data);

    return $conn->lastInsertId('âg');*/

    // TODO
}